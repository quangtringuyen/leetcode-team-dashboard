# ===========================================
# AWS S3 STORAGE CONFIGURATION
# ===========================================
# Leave empty to use local file storage instead of S3
# Uncomment and fill in your credentials to use S3:
# AWS_ACCESS_KEY_ID=your-aws-access-key-here
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key-here
# AWS_DEFAULT_REGION=ap-southeast-1
# S3_BUCKET_NAME=your-bucket-name
# S3_PREFIX=prod

# ===========================================
# SECURITY (REQUIRED FOR FASTAPI BACKEND)
# ===========================================
# JWT token signing key - KEEP THIS SECRET!
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-here-generate-a-new-one

# ===========================================
# FASTAPI APPLICATION SETTINGS
# ===========================================
ENVIRONMENT=production
LOG_LEVEL=info

# CORS Origins (comma-separated list of allowed origins)
# For local development:
# CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8090
# For production (update with your actual domain):
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8090,https://quangtringuyen.cloud,http://quangtringuyen.cloud,https://api.quangtringuyen.cloud,http://api.quangtringuyen.cloud

# JWT Token expiration time in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# ===========================================
# FRONTEND API URL CONFIGURATION
# ===========================================
# For local development:
# VITE_API_URL=http://localhost:8090
# For production deployment:
VITE_API_URL=https://api.quangtringuyen.cloud

# ===========================================
# SCHEDULER CONFIGURATION
# ===========================================
RUN_ON_STARTUP=false
